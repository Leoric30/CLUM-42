/**
 * App.tsx ya no se usa directamente — el punto de entrada es main.tsx,
 * que monta <Providers> + <AppRouter>.
 *
 * Este archivo puede eliminarse o reutilizarse para un layout raíz
 * si en el futuro se necesita una shell global (navbar, sidebar, etc.)
 */
export default function App() {
  return null
}
